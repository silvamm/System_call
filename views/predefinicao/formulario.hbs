{{#section 'css'}}
{{/section}}

<section class="card">
    <div class="card-header">Salvar Predefinição</div>
    <div class="card-body">
        <form class="" action="/predefinicao/" method="post" id="form">
            <input type="hidden" name="id" value="{{predefinicao.id}}" />

            <div class="form-group">
                <label for="nome">Nome:</label>
                <input class="form-control" type="text" name="nome" value="{{predefinicao.nome}}">
            </div>
           
            <div class="float-right">
                <a type="submit" class="btn btn-danger" href="/predefinicao/lista">Cancelar</a>
                <button type="submit" class="btn btn-success">Cadastrar</button>
            </div>
        </form>
    </div>
</section>

{{#section 'js'}}
{{/section}}